# 独立模型设置与商品整理改造

## 当前交付边界

2026-09-11 已完成本地实现与验收，交付到独立验证分支；本文不代表已经部署上线。没有新增付费模型请求，没有请求出海匠、TokScript或飞书，没有修改生产数据、账号或HTTPS。云端隔离验收和上线状态以对应提交的工作流结果及后续部署记录为准。

## 已确认的业务规则

| 用途 | 初始配置 | 单次超时 | 默认额外重试 |
| --- | --- | --- | --- |
| 商品基础资料 | Qwen / qwen3.7-plus | 120秒 | 0 |
| 手卡视频分析 | 沿用现有手卡视频模型；无历史设置时为 qwen3.5-omni-plus | 600秒 | 1 |
| 中文翻译 | 沿用现有翻译模型；无历史设置时为 qwen-plus | 60秒 | 0 |

- 三种用途可分别选择服务商、模型，沿用共享地址/密钥或填写独立配置；每种用途最多额外重试一次。
- 手卡视频始终发送完整本地MP4含原音轨，保留原两视频并发、单任务停止及行级写回保护。不增加任务安排表的视频分析。
- 商品使用已有出海匠图文，不重新分析商品链接。正常建卡每PID仍只取数一次；同PID所有正常补录按钮复用整理缓存。
- 保存模型设置不产生模型请求，不自动更新历史资料。一次视频/商品/翻译请求循环持有同一配置快照，重试中不会换模型。
- 中文翻译继续独立处理口播，优先使用已有中文；视频失败不撤销翻译或视频文件。

以上为根据项目负责人确认的目标；具体实现和验证边界见下文。

## 代码入口与最小修改范围

| 位置 | 职责 |
| --- | --- |
| `lib/ai/types.ts` | 三用途配置定义、地址/模型/重试校验 |
| `lib/ai/settings.ts` | 用途设置加密保存与运行时读取，不静默切换模型 |
| `lib/ai/shared-settings.ts` | 共享服务商配置；更换地址时要求重新输入或清除旧密钥 |
| `app/admin/providers/` | 设置界面、服务端表单；不下发实际密钥 |
| `lib/products/catalog-analyzer.ts` | 商品图文提示词、接口适配、响应保存、格式及来源编号校验 |
| `lib/products/catalog.ts` | 自动一次取数/整理与持久缓存；缓存命中不要求模型凭据 |
| `lib/products/catalog-reorganize.ts` | 明确点击后的仅缓存重新整理、幂等任务、原结果保护 |
| `lib/providers/qwen.ts` / `lib/analysis.ts` | 手卡完整视频与独立翻译实际调用，原视频调度和停止保护保留 |
| `lib/admin-auth.ts` / `lib/require-admin.ts` / `proxy.ts` | 后台登录、请求拦截、敏感服务端操作再鉴权 |

旧按用途模型编辑组件及不再被调用的数据库写入/列表函数已移除；旧模型表的读取保留，用于迁移前配置和仍在用的完整报告/配音链路。未删除旧表或历史配置。数据库启动时不再删除OpenAI服务商行。

## 配置优先级和鉴权位置

- 已保存 `ai_purpose_settings` 时，以该用途的设置为准，不再被旧 `QWEN_VIDEO_MODEL` 覆盖。
- 未保存新用途设置时，手卡视频依次读取旧环境变量、旧手卡用途模型、共享视频模型、无配置初值；显式设置无效时报错，不跳过它换模型。翻译不继承视频模型。
- 新用途配置在 MySQL `ai_purpose_settings.config_json`，独立密钥在其 `encrypted_api_key`；共享配置仍在 `provider_settings`。加密主密钥位置为私有卷 `/app/.data/secret.key`。
- OpenAI共享配置可沿用旧 `OPENAI_API_KEY`，但环境变量密钥只发往官方 `https://api.openai.com/v1`。后台清除操作不删除服务器环境变量；停用共享服务商可阻止使用。更换网关须明确填写相应密钥。
- 本地保存的配置快照只在任务内存中存在，不把实际密钥放入视频诊断、模型提示词或前端。
- `qwen3.7-plus`只作为商品图文模型，不作为已经验证能听原音轨的视频模型。自定义视频模型须明确确认完整音视频能力；确认框不是自动验收证明。

## 商品响应兼容和事实边界

真实诊断响应曾返回“只有一个商品的数组”，商品内PID正确；引用编号还附了说明，导致原程序误判。新校验允许单对象或恰好一个对象的数组，继续拒绝多商品、嵌套数组、错误PID和浮点数PID。来源只接受已提供的编号，允许已知编号后的说明后缀；未知引用被排除并警告，没有有效引用的字段保持缺失。只允许人群/场景作有依据的推断。

已把PID和来源编号写进枚举约束；加强尺寸对应整机/内瓶/配件、宣传在正文标注、推断单独标注等提示词。不使用商品名字或链接英文名猜测身份。

**格式或来源编号通过，不等于语义完全准确。** 上一轮实测模型把内瓶尺寸归给绿色外壳；本次没有追加真实模型调用，不能宣称新提示词已彻底消除图片误读。保存HTTP成功原始响应供离线排查，避免因为没留证据而再次付费。

商品重试仅针对请求网络失败、408/429/5xx。格式、PID或来源校验失败先保留响应，停止本次整理；不为这些问题自动重复收费。永久自动请求标记不删除、不重置。

## 旧资料重新整理

后台“产品与手卡”中输入PID，勾选费用确认后执行：

1. 必须已有成功取数的商品缓存，原整理不能仍在进行。
2. 仅从已保存文字、图片构建输入；缺图提示缺失，不重新下载，更不会调用出海匠。
3. 同一提交标识持久幂等；同PID使用数据库锁。进程内串行处理手动重整，限制图片内存占用。
4. 成功结果先保存到私有 `reorganizations/<提交标识>/organized.json`，再事务发布到PID缓存；失败保留原结果和历史文件。
5. 原响应、原整理结果、自动请求标记均不删除。已落盘但数据库发布失败时，重放同一提交可恢复，不重复请求模型。
6. 本操作只更新商品缓存，不自动改所有手卡。完成后回飞书对应行点“补录手卡”，按原写回保护规则更新该手卡。

不确定是否完成的历史重整任务会阻止新付费重整，需要管理员先核查，不能靠刷新或删标记自动重试。

## 登录与HTTPS上线前置要求

- 使用浏览器原生账号密码登录框。账号及加盐scrypt密码哈希保存在 `/app/.data/admin-auth.json`，权限0600；没有硬编码密码，没有提交实际凭据。每来源15分钟最多10次失败，密码校验并发上限4，成功校验短时缓存。
- 新代码未配置登录时拒绝后台访问；生产HTTP后台不索取明文密码，仅接受HTTPS。设置页和重整服务端操作额外验证登录。
- 飞书自动化、手卡同步及三个字幕桥接POST接口不要求后台账号，继续检查原有自动化/字幕密钥。字幕密钥缺失时明确拒绝，不再把“未配置”当成授权。原媒体读取、产品图片和健康探针保持可用。
- 生产上先准备可信HTTPS及自动续期，再配置负责人指定的账号密码，最后部署代码。脚本 `scripts/configure-admin-auth.mjs` 通过不回显的交互输入设置账号密码；不能把实际密码放进命令、Git或文档。
- Nginx必须覆盖 `X-Real-IP`、`X-Forwarded-Proto`，应用继续只暴露127.0.0.1:3000。现有HTTP飞书POST路径不可被301重定向成GET；应保留原代理路径或逐个验证更新自动化地址。HTTPS后的浏览器后台才启用登录。
- 这次不直接合并main自动部署。先推送验证分支，通过Linux镜像、隔离MySQL和登录/机器接口冒烟验收后，再进行生产切换。

## 验证与限制

- 本次发布相关的23个测试文件共253项测试全部通过，0失败、0跳过；覆盖原有视频、飞书写回、商品缓存、部署保护，以及新增设置和登录。测试使用隔离样本/模拟接口，没有访问生产服务。这不等于仓库全部历史测试或线上验收均已完成。
- 最终源码已通过 `npm run check`、本次变更相关的ESLint、`git diff --check` 与生产构建。构建出现过的过宽文件扫描警告已修复，最终构建无该警告。设置页另用真实React组件、虚拟配置进行了本地渲染检查，未提交任何表单。
- 已加入结构异常、精确PID、实际图像字节、独立用途、加密/掩码、错误地址换钥、重试上限、手动幂等/故障恢复、登录和真实Next请求拦截等无付费测试。
- 隔离MySQL测试新增配置表迁移与跨连接锁检查，仍只允许固定的CI测试数据库。当前Mac未提供Docker，该部分已在下述云端验证分支执行通过，并非在生产数据库运行。
- 旧完整报告与配音音轨转写保留自己的旧用途/固定音频模型，不冒充三种新设置均控制所有历史实验入口。
- 未向Qwen、OpenAI或商品供应商追加请求；界面或模拟测试通过不能代替任意自定义模型的真实能力验收。

接口格式参考：[阿里云结构化输出](https://help.aliyun.com/zh/model-studio/qwen-structured-output)、[OpenAI结构化输出](https://developers.openai.com/api/docs/guides/structured-outputs)。请求拦截与服务端鉴权分别处理，参考[Next.js Proxy](https://nextjs.org/docs/app/api-reference/file-conventions/proxy)。

## 2026-09-11 云端验收与上线前检查

- 已验证的代码提交：`f6e1190cf02a3924521fe8cc9d50626894fa5c36`，分支 `codex/verify-handcard-20260909`。[验证运行记录](https://github.com/lihuiming3216-droid/viral-video-analyzer/actions/runs/34523473058)成功完成；Linux镜像构建、253项离线回归、2项真实MySQL测试及应用/数据库启动检查通过，测试均无失败和跳过。验证环境未配置生产或付费服务凭据。
- 生产应用和数据库健康，应用仍为 `aef72b68f5a4debed36039e0543f30b420e965d6`；main未变，未部署新代码。原自动化和字幕接口密钥均存在，仅检查了是否配置，没有输出密钥值。
- Nginx仅监听HTTP，尚无正式HTTPS证书或后台账号。短时、无业务内容的TLS检查在服务器内部返回200，但外部连接失败；这只能确认外部连通性尚未通过，不能直接断言是Lightsail防火墙所致。临时监听和自签诊断证书已撤除，Nginx未改动。AWS控制台当前停在登录页，需负责人登录后核查443规则。
- 磁盘约6.8GB可用、使用率89%；存在大量历史程序镜像。尚未清理任何程序镜像、数据库、视频或手卡缓存。新版本导入前需核算空间；如需清理旧程序镜像，先取得许可，并保留当前版本、上一正式版本及所有现存容器依赖的镜像。
- 不强制购买域名：证书机构已支持IP地址证书，但必须安排自动续期并验收。参考[Let's Encrypt IP证书说明](https://letsencrypt.org/2026/03/11/shorter-certs-certbot)。端口规则核查参考[AWS Lightsail防火墙说明](https://docs.aws.amazon.com/lightsail/latest/userguide/amazon-lightsail-editing-firewall-rules.html)。后台地址选择仍待负责人回复。
