# 手卡视频收尾：独立CI记录

日期：2026-09-10。本轮获准提交独立测试分支并运行Linux验收，不包含合入主分支或部署。

## 被测版本与范围

- 分支：`codex/verify-handcard-20260909`。
- 被测提交：`09c276eb7553a9f05581a3f4ef4cf0318f48bea8`。
- [GitHub Actions运行34440482905](https://github.com/lihuiming3216-droid/viral-video-analyzer/actions/runs/34440482905)。
- 业务修改只有`lib/feishu/product-doc-sync.ts`：将标准无口播标记在空白翻译格中显示为“无口播”，不改变存储数据、普通译文、完整视频分析或非空保护。
- 4个测试文件补充并发隔离、无口播仍分析完整视频、媒体音轨完整性和精简标签检查。既有测试清单已覆盖，无新增依赖或工作流修改。

## 验收结果

推送前本地指定13个测试文件共125项通过，TypeScript与相关文件ESLint通过。

本次Linux作业已实际成功，开始于2026-09-10 05:17:37 UTC，结束于05:23:06 UTC，耗时5分29秒。运行API的`headSha`与上述被测提交一致。

| 检查 | Linux结果 |
|---|---|
| 镜像内TypeScript检查及Next生产构建 | 通过 |
| 部署与验收流程防护 | 24通过、0失败、0跳过 |
| 手卡视频回归 | 101通过、0失败、0跳过 |
| 隔离空白MySQL及应用启动检查 | 通过 |

合计125项测试通过、0失败、0取消、0跳过；不是全部历史测试或所有业务场景均已验收。未运行全库ESLint。

构建仍有16条Turbopack警告；Checkout还有Node20弃用、被GitHub强制使用Node24的提示，镜像系统安装也报告一个手册软链接警告。未关闭检查来隐藏警告，也没有在本轮扩大修改范围。

仅推送测试分支，主分支未更新，未创建生产部署。后续补录本Markdown的提交不改变被测源码；工作流排除纯Markdown更新，因此不重复触发构建。

## 边界

- 测试中的数据库、视频和飞书响应使用隔离资源或替身，不向真实手卡写入，不调用付费提供方；生产数据与凭据不进入本轮提交。
- 没有口播不等于没有声音。音乐和音效仍属于完整视频分析输入；缺少音轨的文件仍拒绝，未实现原生无音轨的来源判定。
- 飞书最终读取后、写入前发生人工编辑仍可能竞争。此前已确认保留现有写回策略，本轮没有消除该限制，不能以测试通过声称绝不会覆盖人工内容。
- 原有未提交审计文档和内部现场记录继续留本地。本记录仅包含可公开的代码范围与CI结果。

## 后续：构建警告处理（2026-09-10，本地验收）

本节是 `c77162b` 之后修改的本地验收快照；记录时尚未提交或推送，不能视作新的 Linux CI 或线上验收。

- `lib/product-parser.ts` 仅为两处 `existsSync` 参数增加精确的 `turbopackIgnore` 标记：浏览器程序由运行环境安装，登录资料位于运行时数据目录，不属于代码构建应收集的资产。运行时存在性检查保留，没有全局关闭警告或排除正常依赖。
- 对比修改前后 TypeScript 转换为 JavaScript 并去除注释的结果，内容完全一致；浏览器选择顺序、资料目录检查、失败恢复和手卡业务规则未改变。
- 两个工作流的 Checkout 更新为 [actions/checkout v7.0.1](https://github.com/actions/checkout/releases/tag/v7.0.1)，锁定经 GitHub API 核验的完整提交 `3d3c42e5aac5ba805825da76410c181273ba90b1`。该提交的 `action.yml` 声明使用 Node 24。触发条件、只读权限、`persist-credentials: false` 和部署步骤均未改变。

本地 macOS / Node 22.23.0 / Next 16.2.11 下，修改前生产构建复现同类警告 18 条，修改后为 0 条。上文 Linux 的 16 条属于不同环境的上次运行记录，不能混作本轮 Linux 结果。

| 本地构建产物检查：商品解析接口文件追踪清单 | 修改前 | 修改后 |
|---|---:|---:|
| 收集文件数 | 487 | 303 |
| 原始 app/lib 文件数 | 99 | 1 |
| 测试文件数 | 33 | 0 |
| 是否误收集根目录 next.config.ts | 是 | 否 |

修改后检查全部 48 份追踪清单，没有任何一份误收集根目录 `next.config.ts`。上述数字仅说明文件追踪范围改善，不代表完整 Docker 镜像大小或运行内存下降；两次本地编译均约 3.5 分钟。

最终验收：原有 13 个部署/手卡测试文件加 `tests/product-parser-evidence.test.mjs`，共 180 项通过、0 失败、0 取消、0 跳过；包含本轮新增的 3 项浏览器运行路径检查和 1 项 Checkout 版本防护。生产构建、构建内 TypeScript 检查、3 个修改代码/测试文件的 ESLint 与 `git diff --check` 均通过。

本机无 Docker，本地验收阶段尚未重跑 Linux 镜像、空白 MySQL 启动检查或 GitHub Checkout；Node 20 提醒是否消失需后续 CI 实际确认。此阶段没有修改系统手册软链接提示，没有调用付费模型接口、写入真实飞书文档、提交、推送或部署。后续经确认推送独立测试分支，再运行 Linux 验收。

## 警告修复：Linux 验收完成（2026-09-10）

负责人随后确认继续，已将上述 6 个文件提交并推送到独立测试分支，未合入主分支或部署。

- 被测代码提交：`52a3d4f3b1188b30b1eb39df13d2c076bd9bb39f`。
- [GitHub Actions 运行 34442250484](https://github.com/lihuiming3216-droid/viral-video-analyzer/actions/runs/34442250484)，运行 API 的 `headSha` 与被测提交完全一致，最终状态为 `success`。
- Linux 作业开始于 2026-09-10 05:44:04 UTC，结束于 05:49:33 UTC，耗时 5 分 29 秒。

| 检查 | 本次 Linux 结果 |
|---|---|
| 新版 Checkout 取代码及收尾 | 通过，无 Node 20 弃用或强制切换提示 |
| 镜像内 TypeScript 及 Next 生产构建 | 通过，无原先的 Turbopack 文件追踪警告 |
| 部署与验收流程防护 | 25 通过、0 失败、0 跳过 |
| 手卡视频回归 | 101 通过、0 失败、0 跳过 |
| 隔离空白 MySQL 与应用启动 | 通过 |

Linux 合计 126 项测试通过、0 失败、0 取消、0 跳过。与上文的本地 180 项分开记录：Linux 手卡工作流未包含额外的 54 项商品页面测试，这些已在本地通过，不能称为全部 180 项均已通过 Linux 验收。

完整日志仍有一条 `update-alternatives` 提示，内容是缺少 `xdg-open` 帮助文档而跳过帮助软链接创建；本轮按既定范围未修改。应用刚启动时的一次 readiness 探测未通过，随后在既有启动等待循环中通过，最终启动检查和整个作业均成功。

验收后核对：远端主分支仍为 `59d24f31cecc46780a47f3a3a6dcbef6333d11b8`，最新生产部署仍是此前的运行 `32156586668`，没有触发新部署。其他未提交 README、审计资料及内部记录未被纳入提交。后续补记本节仅改变 Markdown，工作流忽略此类更新，不改变被测代码或重复触发验收。
