# 独立验收记录

日期：2026-09-09。负责人已允许提交到独立测试分支运行，并指定一份手卡允许测试写入；没有授权合入main或部署。

## 边界

- 分支：`codex/verify-handcard-20260909`。包含已经审计的线上恢复基线和本地待验收修复，不把旧Git基底冒充现网源码。
- `.github/workflows/verify-handcard.yml` 只响应指定测试分支的代码推送，不使用生产Secrets、SSH、SCP、镜像推送或发布步骤。文档更新不重复消耗构建资源。
- 复用Dockerfile、现有回归用例及`deploy/smoke-test.sh`；测试进程禁用外网，MySQL与应用只连接临时内部网络、空白数据库和临时媒体目录，不挂载生产数据卷。
- 生产发布仍只响应main；不运行手动发布，不改变现网容器、数据库或应用设置。
- 飞书仅使用负责人本次指定手卡。读取已有结构后再写临时验收区域；原有商品资料、视频及人工文字不修改。模型/队列/业务仓储使用内存替身，真实请求只验证飞书读写接口，不触发付费分析。
- 飞书凭据只在现有服务器进程内解密使用，不复制到本机、GitHub、测试容器或文档，不启动第二个业务服务。

## 执行结果：暂未满足上线条件

- 新增5项流程保护测试通过；连同现有部署防护用例共24项通过。Shell语法、YAML解析和新测试ESLint通过。这不是Linux执行结果。
- 本地完成测试分支提交，但推送被GitHub拒绝：当前OAuth认证缺少`workflow`权限。远端测试分支未创建、CI运行数为0；不能将本地提交或离线测试称为云端验收通过。
- 指定手卡原有87个块、一行完整视频/分析/中文结果。临时区域与现网任务表头不同，只调用实际写回保护函数和文件上传模块；模型、队列、业务数据库写入不参与，付费请求为0。
- 6类真实飞书检查中，空白分析/中文独立回填、多段人工保护、预读后新增人工译文、换链接拒写、实际MP4预览块上传及重复上传保护通过；**最终读写竞争用例失败**。
- 预览检查证明实际文件/预览块绑定成功且不重复上传；没有完成新视频封面与播放声音的浏览器视觉/听感验收。
- 临时验收表及其中的视频引用已移除，清理后复查仍为原有87个块，全部内容及结构逐项一致；仅文档历史版本前进至72。服务器合成的2秒测试MP4已删除，不涉及成功业务媒体。
- 复核应用和MySQL镜像、启动时间及健康状态均未变，没有生产部署。调整测试描述后，手卡43项加流程保护5项共48项本地测试仍通过；实际竞争失败仍单独保留，不能并入“全部通过”。

### 已复现的保护缺口

再次对临时结果格做受控复核：先读取版本68；模拟人工写入后，接口及元数据均确认版本69；随后仍使用68发送自动替换，飞书成功返回版本70，临时人工文字被替换。说明这不是“元数据还没更新”的简单解释，`document_revision_id`不能当成原子条件更新保障。

旧mock会主动拒绝过期版本，因此相关单元测试通过只能证明拒绝后的错误处理，不能证明飞书服务端真的会拒绝。已修正源码注释和测试描述，**没有通过改断言把这个实际缺口伪装成修复完成**，也没有因此删除整格复查。

下一步需确认写回策略：保留原有整格非空检查，同时考虑只新增独立自动段落、不替换已有段落。此方式有望避免覆盖人工原文，但两人同时写入时可能短暂并存两段，需要业务确认与单独验收；多读一次或多等几秒都不能保证消除竞争。

GitHub权限原因见[官方OAuth权限说明](https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/scopes-for-oauth-apps)。飞书[官方SDK参数说明](https://github.com/larksuite/oapi-sdk-go/blob/v3_main/service/docx/v1/model.go)将该参数描述为操作的文档版本，并未在此声明条件写入保障；上述失败结论以本次真实请求为依据。

内部审计与详细手卡快照只保存在操作者本地，不随公开仓库发布。分支及路径过滤规则按[GitHub官方工作流触发说明](https://docs.github.com/en/actions/how-tos/write-workflows/choose-when-workflows-run/trigger-a-workflow)配置。
